import { DecToTimePipe } from './dec-to-time.pipe';

describe('DecToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DecToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
